<div class="{{$class}}">
    <div class="{{$labelClass}}">{{$label}}</div>
    <input type="{{$type}}" name="{{$name}}" placeholder="{{$placeholder}}" value="{{$value}}" />
    <small>{{$error}}</small>
</div>
